{
  "name": "kloudless-file-explorer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon -d 1 --watch ../dist/ --watch . server.js",
    "ssl": "SSL_KEY=key.pem SSL_CERT=cert.pem nodemon -d 1 --watch ../dist/ --watch . server.js"
  },
  "dependencies": {
    "express": "4.9.0",
    "jade": "1.6.0",
    "morgan": "1.3.1",
    "nodemon": "^1.9.1",
    "stylus": "0.48.1"
  }
}
